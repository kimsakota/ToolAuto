<UserControl x:Class="WinApp.Views.ScriptToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinApp.Views"
             xmlns:vst="clr-namespace:Vst.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="vst:SvgIcon">
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border x:Name="indicator" 
                Width="16" 
                Height="16" 
                Background="Red"
                HorizontalAlignment="Left"
                Visibility="Collapsed"
                Margin="20 0"
                CornerRadius="10"></Border>
        <Grid x:Name="buttonsPanel">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <vst:SvgIcon Source="open" Grid.Column="0" />
            <vst:SvgIcon Source="save" Grid.Column="1" />
            <vst:SvgIcon Source="play" Grid.Column="2" />
            <vst:SvgIcon Source="record" Grid.Column="3" />
        </Grid>
    </Grid>
</UserControl>
