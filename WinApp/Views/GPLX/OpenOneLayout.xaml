<UserControl x:Class="WinApp.Views.GPLX.OpenOneLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vst="clr-namespace:Vst.Controls"
             xmlns:local="clr-namespace:WinApp.Views.GPLX"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="White" BorderBrush="LightGray" BorderThickness="0 1 0 0">
            <StackPanel 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                <vst:Button Text="Xong" Url="/xong" Background="Red" />
                <vst:Button Text="Hủy" Url="/index" Background="Black" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
